/** Sans-serif family token mapping for UI typography layouts */
export const fontSans = {
  variable: "--font-sans",
};

/** Monospace family token mapping for code blocks and structural metadata */
export const fontMono = {
  variable: "--font-mono",
};
